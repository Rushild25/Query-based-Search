from __future__ import annotations


def card(content: str) -> str:
    return f'<div class="card">{content}</div>'


def card_content(content: str) -> str:
    return f'<div class="card-content">{content}</div>'
